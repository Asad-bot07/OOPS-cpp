#include <iostream> // Preprocessor directive to include input-output stream
using namespace std; // Allows usage of standard namespace (e.g., cout, cin) without std:: prefix
int main() 
{ // Main function - execution starts here
    cout << "Hello, World!"; // Output statement to print text to the console
    return 0; // Return 0 indicates successful program termination
}
